import { PageWrapper } from './PageWrapper';

export const Search = () => {

  return (
    <PageWrapper
      breadcrumbs={[
        {
          path: '/',
          label: 'Home'
        }
      ]}
    >

    </PageWrapper>
  );
};
